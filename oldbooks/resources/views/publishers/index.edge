@layout('base')

@section('content')
  <div class = "container">
    <h1> Authors</h1>
    <table class="table">
      <tr>
        <th>ID : </th>
        <th>Name</th>
        <th>Email :</th>
      </tr>
      @each(p in publishers)
      <tr>
        <td>{{p.id}}</td>
      <td>{{p.name}}</td>
      <td>{{p.email}}<a href="{{route('delete_publisher',{publisher_id})}}">X</td>
      </tr>
      @endeach
      </table>
    </div>

@endsection
